Video Analysis for Estimating Customer Traffic in Retail

The project consists of three almost independent parts:
a pipeline for multi-camera tracking (root directory), metric calculation and ReID model fine-tuning (metrics), and the construction of an analytical dashboard (dashboard).

Examples of how the project components work can be found in the examples folder.

Examples of execution and logs can be found in the usage_example notebook.
